# Changesets

This directory contains changeset files that describe changes to the package.

## How to use

When you make changes to the package, run:

```bash
npm run changeset
```

This will prompt you to describe your changes and will create a changeset file.

When ready to release, run:

```bash
npm run changeset:version
npm run changeset:publish
```
